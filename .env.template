# Firebase Configuration
# Replace these values with your actual Firebase project credentials
# Then rename this file to .env

# Firebase project settings (for hosting)
REACT_APP_FIREBASE_API_KEY="your-api-key"
REACT_APP_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
REACT_APP_FIREBASE_PROJECT_ID="your-project-id"
REACT_APP_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
REACT_APP_FIREBASE_MESSAGING_SENDER_ID="your-sender-id"
REACT_APP_FIREBASE_APP_ID="your-app-id"

# Note: In this frontend-only application, these credentials are only used
# for Firebase hosting and will be exposed in the client-side code.
# This is normal for Firebase hosting configurations.
# No sensitive backend operations will be performed.
